name: hello-world-hello
on: push
jobs:
  hello-word-job:
    runs-on: ubuntu-latest
    steps: 
      - name: check out respository code
        uses: action/checkout@v3  
      - run: echo $("cat hello_wrld_.txt")
      - name: use node.js
        uses: acrion/setup-node@v3
        with:
          node-version: '18.x'
      - run: npm install 
      - run: npm run build

        
      
      
      
